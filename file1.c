#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

#include "file1.h"

struct dataBase* createEmptyDataBase(int numberPeople){

    struct dataBase* base = malloc(sizeof(struct dataBase*);

    if (base == NULL){
        return NULL;
    }
    


}